# TreeFlow 与主流 AI 产品能力对比分析

> 整理时间：2026-09-10
> 对比对象：TreeFlow（本项目）、豆包 / DeepSeek（模型厂商产品）、通用 Agent 产品

---

## 一、结论先行

**TreeFlow 和豆包 / DeepSeek 根本不在同一层。**

- 豆包、DeepSeek（以及 OpenAI、阿里云通义）是 **模型厂商**：自己训练模型、自己建算力集群、自己部署推理服务。
- TreeFlow 是 **编排层 / 应用层客户端**：本身没有任何模型、没有一行推理代码，只是在别人家的 API 上做「上下文管理 + 会话状态管理 + 可视化」。

一句话类比：

> 豆包 / DeepSeek 是 **造发动机并卖车**；TreeFlow 是 **自己设计驾驶舱**，发动机向多个厂商采购。

---

## 二、底层区别：谁在调用谁

### 2.1 分层栈

自下而上是一个栈，每上一层，价值就从「文本」转向「行动」：

```
⑥ 平台/生态层   多用户、分享、模板市场、插件/MCP 生态
⑤ 多 Agent 层   角色分工、工作流/DAG、自动调度、评审汇总
④ Agent 层      工具调用、多步循环、记忆、RAG、流式
③ 编排层(上下文) TreeFlow 现在在这里：树 + 历史重建 + 路由
② 推理服务层    部署、KV Cache、批调度（豆包/DeepSeek 自家）
① 模型层        权重、训练、对齐
```

### 2.2 TreeFlow 的真实调用链

```
用户发消息 → TreeFlowAgent.ask()
   ↓
ConversationTreeManager.getConversationHistory()
   （把当前树分支路径还原成 messages 数组）
   ↓
ApiManager.ask()  按 provider 分发
   （Ollama / 阿里云 / 百炼 Agent / 通用 OpenAI 兼容）
   ↓
POST 到对方的 /chat/completions，拿回一段纯文本
   ↓
文本写回树节点，并渲染成脑图
```

对应代码位置：

- `server/core/agent/TreeFlowAgent.js` → `ask()`
- `server/core/managers/ConversationTreeManager.js` → `getConversationHistory()`
- `server/core/managers/ApiManager.js` → `ask()` / `askOpenAICompat()`

本质：**TreeFlow = HTTP 客户端 + 一棵树 + 一个状态机**。真正的「智能」完全发生在对方的服务器上。

### 2.3 豆包 / DeepSeek 也不是「裸调模型」

它们的产品 App 同样有一层编排（系统提示词、RAG 检索、工具调用、安全审核、记忆压缩、流式渲染）。区别在于：

| 维度 | TreeFlow | 豆包 / DeepSeek |
|---|---|---|
| 模型权重 | 不存在 | 自研自训（预训练 + SFT + RL） |
| 推理算力 | 0 | 自建 GPU 集群、自研推理引擎 |
| 推理执行位置 | 别人服务器 | 自己服务器 |
| 代码性质 | 客户端 + 状态管理 | 全栈：模型 + 服务 + 产品 |
| 上下文范式 | **树状分支**（回溯 / 引用 / 覆盖重答） | 线性会话 + 记忆 |
| 模型可替换性 | 完全可换（provider 无关） | 绑定自家模型 |
| 成本结构 | 按 API 计费 | 自摊算力成本 |

> 「他们都是直接调用 LLM 吗」——从应用代码看，是的，大家都在调 LLM 接口；
> 真正的分野是 **模型是谁的、跑在哪、能不能换**。

---

## 三、能力矩阵对比

`✓` = 有，`✗` = 没有，`~` = 部分 / 有限

| 能力 | TreeFlow（现状） | 豆包 / DeepSeek App | 通用 Agent 产品¹ |
|---|---|---|---|
| 自研模型 / 算力 | ✗ | ✓ | ✗（多借第三方） |
| 上下文编排 | ✓ | ✓ | ✓ |
| **树状分支结构** | ✓ | ✗（线性） | ✗（线性） |
| 可回溯 / 覆盖重答 | ✓ | ~ | ~ |
| 多 provider 可换 | ✓ | ✗（绑自家） | ✓ |
| 流式输出 | ✗ | ✓ | ✓ |
| 工具调用 | ✗ | ✓ | ✓✓ |
| 多步自主循环 | ✗ | ~ | ✓✓ |
| 记忆 / 长期召回 | ✗ | ~ | ✓ |
| RAG 知识检索 | ✗ | ✓ | ✓ |
| 步骤可视化 | ✓（脑图） | ✗ | ~（时间线） |
| 多 Agent 协作 | ✗ | ✗ | ✓ |

¹ 指以「能自己动手多步完成任务」为核心的产品形态（各类 Agent / 深研类产品）。

---

## 四、同一任务的执行流程对比

任务：`帮我分析这两篇论文的主要差异`

**TreeFlow**

```
1 次请求 → 模型凭上下文直接答 → 写成 1 个节点
（想深入？人去手动点「分支」再问）
```

**豆包 / DeepSeek**

```
1 次请求 → 可能联网检索 → 流式吐字 → 回答结束
（一条线性会话，重答覆盖原答案）
```

**通用 Agent 产品**

```
拆解任务 → 检索/读文件 → 中间观察 → 修正方向 → 汇总报告
（耗时几十秒到几分钟，中间步骤可审计）
```

三方都在「回答问题」，但：

- 只有 **Agent** 在「自己动手做事」；
- 只有 **TreeFlow** 把结果存成 **可回溯的树**。

---

## 五、能力清单：别人有 vs TreeFlow 现状

### A. 输入侧（多模态）

- 图片理解（截图、拍照、图表）`✗`
- 语音输入（实时语音对话）`✗`
- 文件解析：PDF / Word / Excel / PPT / CSV `✗`
- 视频 / 音频理解 `✗`
- OCR、网页链接抓取 `✗`

### B. 输出侧

- 流式逐字输出 `✗`（当前 `stream: false`）
- 富渲染：表格、LaTeX 公式、Mermaid、图表 `~`（仅 Markdown 文本）
- 代码块执行（沙箱跑出结果）`✗`
- 直接产出文件：docx / xlsx / pptx / pdf `✗`
- 语音播报（TTS）`✗`
- 图像 / 视频生成 `✗`

### C. 知识 / 检索

- 联网搜索（实时信息）`✗`
- 上传知识库做 RAG（向量检索）`✗`
- 引用来源标注（答案附链接 / 页码）`✗`
- 深度研究（Deep Research 式长任务）`✗`

### D. 动手 / 执行

- Function Calling / 工具调用 `✗`
- 代码解释器 / 沙箱执行 `✗`
- 浏览器自动化（点网页、填表）`✗`
- MCP / 插件生态 `✗`
- 操作本地系统 / 读写文件 `~`（只有 JSON 数据）
- 定时任务 / 后台长任务 `✗`

### E. 上下文 / 记忆

- 超长上下文窗口（128K ~ 1M）`~`（取决于所调模型）
- 自动摘要压缩（超长历史不爆）`✗`（目前全量塞进 messages）
- 跨会话长期记忆 `✗`
- 自定义指令 / 角色 / 人格 `~`（skills 算最简版）
- 项目 / 工作区（Projects，绑定文件与指令）`~`（topic 算雏形）

### F. 交互 / 协作

- 分享会话（公开链接）`✗`
- 多人协作、评论 `✗`
- 消息编辑 / 重新生成 `✓`（已实现的 reanswer）
- 重新生成多版本对比（同问题 N 个答案并排）`~`（树能表达，但需手动）
- 消息引用、@ 引用上下文 `✓`（引用分支）

### G. 平台 / 工程（用户感知弱但很关键）

- 用户账号 / 鉴权 / 多租户 `✗`
- 用量统计、配额、计费 `✗`
- 成本路由（简单问题走小模型）`✗`
- 失败重试 / 降级 / 限流 `~`
- Prompt 缓存（省钱又加速）`✗`
- 可观测性（trace、token 统计、延迟监控）`~`（healthStatus 算摸到一点）
- 结构化输出（JSON Schema 强制格式）`✗`

### H. 高级推理（最值得关注）

- 深度思考 / CoT 显式推理 `✗`
- 并行采样 Best-of-N（同问题生成 N 个再挑）`✗`
- 自一致性 Self-Consistency（多路径投票）`✗`
- 思维树 ToT / 思维图 GoT（多分支探索 + 评估 + 回溯）`~`（**树结构就是这个的壳**）
- Critic / 自我反思修正 `✗`

### I. 生态

- 公开 API / SDK 给别人用 `✗`
- 提示词 / 模板市场 `✗`
- 社区分享与 fork `✗`

---

## 五·补：E 项（上下文 / 记忆）深度对比

### E.0 核心差异一句话

> 别人的上下文是「**记录**」（一条不断被压缩、覆盖、遗忘的会话日志）；
> TreeFlow 的上下文是「**路径**」（从当前节点沿 parentId 回溯到 root 的一条链）。

代码证据（`server/core/managers/ConversationTreeManager.js`）：

```js
getConversationMessages(topicId, endNodeId = null) {
  // 从 currentNode（或指定节点）沿 parentId 一路回溯到 root
  const tracePath = (node) => {
    if (!node || node.id === 'root') return;
    const parent = node.parentId ? this.findNodeById(topic.conversationTree, node.parentId) : null;
    if (parent) tracePath(parent);
    if (node.message)  path.push({ type: 'user', content: node.message,  nodeId: node.id });
    if (node.response) path.push({ type: 'ai',   content: node.response, nodeId: node.id });
  };
  tracePath(current);
}
```

这意味着：**不在当前路径上的分支，一个字都不进上下文**。切分支 = 换上下文。

### E.1 子维度对比

| 子维度 | TreeFlow | 豆包 / DeepSeek | 通用 Agent |
|---|---|---|---|
| E1 上下文拓扑 | **树路径**（多分支互不污染） | 线性会话 | 线性 + 外部记忆 |
| E2 长度管理 | `✗` 无计数、无截断、无摘要，全量塞 | 滑动窗口 + 自动摘要压缩 | 规划器动态构造 + 压缩 |
| E3 分支 / 回溯 | **原生**（上下文分叉） | 覆盖式重答 | 无 |
| E4 跨会话记忆 | `✗` 换 topic 即清零 | 有（可开关） | 有（向量 / summary store） |
| E5 全局指令 / 人格 | `✗` skills 需手动选，不持久 | Custom Instructions 全局持久 | System Prompt 配置 |
| E6 工作区 / 项目 | `~` topic 算雏形 | Projects + 知识库 | Workspace + 文件绑定 |
| E7 成本效率 | `✗` 无前缀缓存、无裁剪，每轮全量发 | 前缀缓存 + 压缩 | 前缀缓存 + 上下文裁剪 |
| E8 上下文的可见性 | **高**（路径在脑图上完全可视） | 低（黑盒摘要，看不到） | 中 |

### E.2 TreeFlow 独有的两个优势

1. **上下文分叉（Context Forking）**
   同一棵树上的 N 条分支 = N 份互不干扰的上下文。可以拿同一个问题在不同分支里问不同模型、用不同前置条件，天然形成 A/B 对比。别人只能"覆盖重答"，留不下两条并行上下文。

2. **上下文可复现（Reproducible Context）**
   上下文由「路径回溯」纯函数式计算得出，同一路径永远重建出同样的 messages —— 可审计、可回归测试、可精确复现某次回答的输入。别人的压缩历史是**有损且时变**的，做不到。

### E.3 当前的三个缺陷（都很具体）

1. **没有长度管理 → 树越深越容易爆**
   `getConversationHistory()` 无 token 估算，历史随深度线性增长，最终会撞上模型上下文上限直接报错。全仓库没有任何 `truncate / summarize / max_tokens / tokenCount` 逻辑（`max_tokens` 只出现在 Token 健康检测的 ping 请求里）。

2. **`quoteNodeIds` 从未进入上下文 → 多引用目前只是"标记"**
   代码里 `quoteNodeIds` 只做三件事：存到节点、返回给前端、用于渲染连线。**从未参与 messages 构建**。上下文始终由 `parentId` 唯一路径决定，所以「引用多个节点」在 AI 眼里其实只看到了其中一个。

3. **历史全量躺在 topics.json 里，却没有被当成记忆用**
   本地握有全部对话数据（这是别人没有的资产），但既不检索、也不召回、也不统计，换 topic 就等于失忆。

### E.4 演进路径（按性价比排序）

1. **上下文预算器**：发送前估算 token，超过阈值触发处理（最小改动，防爆）
2. **摘要节点落盘到树**：把压缩结果存成可见节点，可回溯、可人工修正（别人做不到，因为压缩结果不可见）
3. **多引用真正注入**：把 `quoteNodeIds` 的内容拼进 context，并带来源标注
4. **跨话题记忆**：topics.json → 检索式召回（embedding 或轻量 BM25）
5. **上下文可视化编辑**：发送前在脑图上勾选"哪些节点进 context"——**只有树结构能做到这件事**

---

## 六、关键洞见

1. **差距不在「会不会说话」，在「能不能自己动手 + 自己循环」。**
   与豆包 / DeepSeek 的差距在第 ① 层（模型）和第 ② 层（算力），这部分补不齐，也不该补。
   与 Agent 产品的差距在第 ④ 层（执行闭环）：`tools` 参数、多步循环、结果回灌。

2. **上面 40 多项里，绝大多数是工程堆料**，补起来只是时间问题，且没有一项能成为护城河。

3. **唯一能拉开身位的是 H 组（高级推理）。**
   注意一件事：o1 / R1 这类「推理模型」内部其实就在做树搜索——采样多条推理路径、自我评估、剪枝回溯——只不过它把这棵树 **藏在黑盒里、不给你看、也不让你干预**。

   而 TreeFlow 恰好把同一件事做成了 **外置的、可见的、可干预的树**：

   > 别人有「隐式的思维树」，TreeFlow 有「显式的思维树」。

4. TreeFlow 的**独占优势**在第 ③ 层的树结构：分支、引用、回溯、覆盖重答，加上 provider 无关、数据本地全在手。

| | 别人强 | TreeFlow 强 |
|---|---|---|
| 模型能力 | 自研或先用最强 API | 一样能调（可随时换） |
| 执行能力 | 工具 + 循环 | 目前无，是短板 |
| **结构能力** | 线性上下文 | **树：可分支、可审阅、可回溯** |
| 数据归属 | 在对方云上 | 本地 JSON，全在自己手里 |

---

## 七、补强优先级建议

| 优先级 | 方向 | 性质 |
|---|---|---|
| 1 | 流式输出 | 体验补齐（工程量小） |
| 2 | 结构化输出（JSON Schema） | 能力地基 |
| 3 | 工具调用（Function Calling） | 执行跃迁的起点 |
| 4 | 并行采样 / ToT（自动分支 + 评审 + 剪枝） | **差异化核心** |
| 5 | 记忆 / RAG | 长期演进 |

**结论**：模型和执行是「能力」，结构是「方法」。能力上追不上也追不齐，但「用树来组织 AI 的思考」这件事，目前主流产品都还没做——这是最值得押注的差异点。

其中最契合 TreeFlow 的方向是第 ④ 项：

- **自动分支**：同一问题让模型并发生成 N 个候选路径，各挂一个分支
- **评审 / 自洽**：新增一棵「评审树」，让模型对分支打分、合并、剪枝
- **可回溯的行动轨迹**：Agent 每步工具调用都落成节点，全程可视化、可回退重来

线性对话的 Agent 是「一条黑盒轨迹」，TreeFlow 可以是「一棵可审阅的决策树」。
